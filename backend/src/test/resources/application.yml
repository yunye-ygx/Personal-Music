server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://192.168.100.128:5433/moodtune
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 123456

  sql:
    init:
      mode: never  # 不执行schema.sql，使用已存在的表

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

  ai:
    openai:
      api-key: sk-4bc19b4f8af04134b52a70f8d26027da
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen-plus
          temperature: 0.7

minio:
  endpoint: http://localhost:9000
  access-key: test
  secret-key: test
  bucket-name: test-bucket

llm:
  api-url: http://localhost:8081
  api-key: test
  model: test
